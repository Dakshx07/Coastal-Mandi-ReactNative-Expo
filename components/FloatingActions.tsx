// FloatingActions.tsx - Placeholder for future Voice Assistant feature
// Voice Assistant is planned for Phase 3 after product launch

import React from 'react';
import { View, StyleSheet } from 'react-native';

// Voice Assistant FAB has been removed for initial launch
// Will be re-added once we have a good client base

export default function FloatingActions() {
    // Currently empty - Voice Assistant planned for future release
    return null;
}

const styles = StyleSheet.create({
    // Styles preserved for future use
    container: {
        position: 'absolute',
        bottom: 100,
        right: 20,
        alignItems: 'center',
        gap: 16,
        zIndex: 100,
    },
});
